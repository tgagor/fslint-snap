name: fslint
version: git
summary: FSlint
description: |
  File System Lint detector and cleaner.
base: core18
confinement: devmode

parts:
  fslint:
    plugin: nil
    # source: https://github.com/pixelb/fslint.git
    stage-packages:
      - fslint

apps:
  fslint-gui:
    command: fslint-gui
